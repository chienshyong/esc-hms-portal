const TenantOptions = require('../page.js');


describe('pages', () => {
 
  test('test for page.js could be test', () => {

    
        // Wrap the function call in a function to catch the error and then throw it
      //   expect(() => {
      //     // if input list is empty, there will be an error
      //     TenantOptions();
      // }).toThrowError('Test error');

         
  })
});