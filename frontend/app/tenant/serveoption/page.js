import Options from "@/components/tenant/options"

export default function ServiceOptions() {
    return (
        <section className="h-full flex flex-col justify-center items-center ">
            <h1 className="mb-1 text-4xl font-extrabold text-center">What can we help you with?</h1>
            <Options></Options>
        </section>
    )
}